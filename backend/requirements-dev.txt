# Development / test dependencies (installed on top of the runtime ones).
-r requirements.txt

pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<0.25.0

# Async ASGI test client (also pulled in by fastapi[standard], pinned here so
# the dev image is explicit about what the test suite relies on).
httpx>=0.27.0,<0.29.0

# In-memory Redis so the CodeStore logic is tested against the real
# redis.asyncio API without needing a running server.
fakeredis>=2.21.0,<3.0.0
